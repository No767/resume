# Resume

A repository that holds my professional resume.

## Generation

To generate the resume, install the necessary dependencies via mise, and generate:

```bash
mise install && mise run generate
```

Resume can be found in `dist/resume.pdf`, which is a version that is ready for public distribution.
